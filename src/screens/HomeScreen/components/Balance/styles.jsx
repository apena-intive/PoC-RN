import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  balance: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    fontWeight: 'bold',
    fontSize: 16,
    textAlign: 'center',
  },
  animation: {
    alignSelf: 'center',
  },
});
